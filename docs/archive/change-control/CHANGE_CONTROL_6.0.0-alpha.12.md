# Change Control — Tlaloc 6.0.0-alpha.12

Status: `IMPLEMENTED_MACHINERY_EXTERNAL_MODEL_EVIDENCE_PENDING`

## Purpose

Promote the already-merged Perception Promotion Campaign R1 machinery to an explicit Tlaloc release without turning mechanical or mock success into an empirical compatibility claim.

## Added capability

- original / 75% / 50% / JPEG-preview carrier transports;
- strict OpenAI-compatible multimodal perception trial runner;
- structured observation packets with no private evaluator ground-truth fields;
- bridge to independent Origami `origami-perception-eval`;
- default 3 models x 3 original real-trial aggregate gate;
- real tool-loop gate using the existing Fixed Carrier R2 executor path;
- held-out routing gate with zero budget violations and `FALSE_EXACT=0`;
- independent Hybrid and Native T3 candidate states.

## Ownership

Tlaloc owns campaign orchestration, transport variation, model execution, repeated trials and promotion recommendations.

Origami owns its visual/semantic contracts and deterministic per-trial carrier evaluation.

Tonal owns aggregate stack composition and final support-state promotion.

## Hard boundaries

```text
MOCK != EMPIRICAL EVIDENCE
ONE REAL TRIAL != SUPPORTED
HYBRID SUPPORTED != NATIVE VISUAL SUPPORTED
T3 NATIVE FAILURE != HYBRID FAILURE
PERCEPTION != VERIFICATION
FALSE_EXACT = 0
```

## Version

`6.0.0-alpha.12`
