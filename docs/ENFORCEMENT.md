# Gatekeeper enforcement

The workflow reports provenance policy. For mechanical multi-user enforcement, GitHub rules for `main` should require both Tlaloc `verify` and `gatekeeper / provenance`. Administrator bypass remains an intentional owner escape hatch unless disabled in repository settings. See Tonal for the canonical policy.
